Compile native using the NDK tool "ndk-build" in the directory. Create a APK by importing the project into Eclipse and selecting Run As -> Android application.

Starting templat based on https://github.com/laurencedawson/native-activity-example.git.
